torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
opencv-python>=4.8.0
matplotlib>=3.7.0
seaborn>=0.12.0
Pillow>=10.0.0
PyYAML>=6.0
tqdm>=4.66.0
thop>=0.1.1.post2209072238
kagglehub>=0.2.0
# ultralytics is only needed if you run the legacy YOLO-based
# detect_bboxes_yolo.py yourself; the default pipeline uses SAM for bboxes
# and does not require it.
ultralytics>=8.1.0
segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
psutil>=5.9.0
